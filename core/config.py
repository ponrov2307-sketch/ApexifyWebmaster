import os
import dotenv

# โหลดค่าจากไฟล์ .env (ถ้ารันบนเครื่องตัวเอง)
dotenv.load_dotenv()

TELEGRAM_TOKEN = os.getenv("TELEGRAM_TOKEN")
GEMINI_API_KEY = os.getenv("GEMINI_API_KEY")
ADMIN_ID = os.getenv("ADMIN_ID")

if not TELEGRAM_TOKEN or not GEMINI_API_KEY:
    raise ValueError("❌ Error: กรุณาตั้งค่า TELEGRAM_TOKEN และ GEMINI_API_KEY ในไฟล์ .env หรือ Environment Variables")

# ==========================================
# 🌟 [เพิ่มใหม่] การตั้งค่าสำหรับหน้าเว็บ (Apex Wealth Master)
# ==========================================

# 1. รายชื่อดัชนีตลาดโลก (สำหรับให้ ticker.py ดึงไปแสดงเป็นแถบวิ่งด้านบน)
MARKET_INDICES = {
    '^GSPC': 'S&P 500', 
    '^SET.BK': 'SET', 
    'BTC-USD': 'Bitcoin',
    'GC=F': 'Gold',
    '^DJI': 'Dow Jones'
}

# 2. ธีมสีหลักของหน้าเว็บ (สำหรับให้ components ต่างๆ ดึงไปใช้ตกแต่ง)
COLORS = {
    "bg": "#0D1117",        # สีพื้นหลังหน้าเว็บ (โทนดำ GitHub Dark)
    "success": "#32D74B",   # สีเขียว (แสดงกำไร / กราฟขึ้น)
    "danger": "#FF453A",    # สีแดง (แสดงขาดทุน / กราฟลง)
    "primary": "#D0FD3E",   # สีเขียวสะท้อนแสงนีออน (ใช้ไฮไลต์หรือปุ่มหลัก)
    "text": "#C9D1D9",      # สีข้อความปกติ
    "card": "#161B22"       # สีพื้นหลังกล่อง หรือ การ์ดต่างๆ
}
