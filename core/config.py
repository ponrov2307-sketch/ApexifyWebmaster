import os
from pathlib import Path
from dotenv import load_dotenv

# 🌟 บังคับหาไฟล์ .env จากโฟลเดอร์หลักของโปรเจกต์เสมอ (แก้ปัญหาหาไฟล์ไม่เจอ)
BASE_DIR = Path(__file__).resolve().parent.parent
ENV_PATH = BASE_DIR / ".env"

if ENV_PATH.exists():
    load_dotenv(dotenv_path=ENV_PATH)
else:
    print(f"⚠️ คำเตือน: ระบบมองไม่เห็นไฟล์ {ENV_PATH} (โปรดเช็คชื่อไฟล์อีกครั้ง)")

# ==========================================
# 🔐 ข้อมูลเชื่อมต่อระบบต่างๆ (API Keys & DB)
# ==========================================
TELEGRAM_TOKEN = os.getenv("TELEGRAM_TOKEN", "")
ADMIN_ID = os.getenv("ADMIN_ID", "")
GEMINI_API_KEY = os.getenv("GEMINI_API_KEY", "")

# ลิงก์เชื่อมต่อฐานข้อมูล
DATABASE_URL = os.getenv("DATABASE_URL")
SUPABASE_URL = os.getenv("SUPABASE_URL")
SUPABASE_KEY = os.getenv("SUPABASE_KEY")

# ==========================================
# 🎨 ตั้งค่าสีและ UI ของหน้าเว็บ (Dashboard)
# ==========================================
COLORS = {
    'bg': '#0D1117',          # สีพื้นหลังหลัก (สไตล์ GitHub Dark)
    'card': '#161B22',        # สีพื้นหลังการ์ด
    'border': '#30363D',      # สีขอบตาราง
    'primary': '#D0FD3E',     # สีเขียวมะนาว (สีแบรนด์ Apexify)
    'success': '#32D74B',     # สีเขียวกำไร
    'danger': '#FF453A',      # สีแดงขาดทุน
    'text_main': '#C9D1D9',   # สีตัวอักษรหลัก
    'text_muted': '#8B949E'   # สีตัวอักษรรอง
}

# ==========================================
# 📈 ดัชนีตลาดโลกที่จะให้วิ่งอยู่บนแถบ Ticker
# ==========================================
MARKET_INDICES = {
    '^GSPC': 'S&P 500',
    '^DJI': 'Dow Jones',
    '^IXIC': 'NASDAQ',
    '^SET.BK': 'SET',
    'BTC-USD': 'Bitcoin',
    'GC=F': 'Gold'
}